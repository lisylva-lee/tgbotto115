# 115 ShareBot 依赖

# [job-queue] 提供 ConversationHandler 的 conversation_timeout（依赖 APScheduler），缺失会导致会话超时无效
python-telegram-bot[job-queue]>=20.0
requests>=2.28.0
# p115client：>=0.0.5。注意不要传 check_for_relogin（PyPI 0.0.9.x 已移除该参数，
# 传了会 TypeError 导致初始化失败；且该参数会触发自动 relogin 可能作废 cookie）。
p115client>=0.0.5
cachetools>=5.0.0
PyYAML>=6.0
